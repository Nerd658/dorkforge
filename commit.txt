[2026-08-19] — Add Container Support with Multi-Stage Dockerfile and Docker Compose Configuration

What changed:
- Added multi-stage 'Dockerfile' compiling static Go binary with stripped debug symbols (-ldflags="-s -w" -trimpath) on Alpine Linux runtime.
- Added non-root unprivileged execution user (appuser:10001) with ca-certificates and tzdata.
- Added 'docker-compose.yml' mapping local './reports' and './targets' volume mounts.
- Added '.dockerignore' to keep the build context lightweight and fast.
- Updated 'README.md' with Docker and Docker Compose build/run instructions.

Why:
- Enable seamless containerized execution in isolated environments, container orchestration, and CI/CD pipelines without requiring local Go toolchain installation.

Files affected:
- Dockerfile — Multi-stage Alpine container build definition
- docker-compose.yml — Compose service and volume definitions
- .dockerignore — Build context exclusion rules
- README.md — Container usage documentation
- commit.txt — Updated release notes

Breaking changes:
- None

Notes for review:
- Build image: 'docker compose build'
- Test scan: 'docker compose run --rm dorkforge scan -d example.com'
