[2026-08-19] — Implement .dfgignore, Report Diffing, Google CSE API, and CIDR/ASN Targets

What changed:
- Added 'internal/ignore' package implementing .dfgignore false-positive and URL pattern exclusion rules.
- Added 'internal/diff' package implementing report delta calculation (--diff previous.json) to highlight new vs resolved findings.
- Added Google Custom Search Engine (CSE) REST API client in 'internal/api/google.go' with --google-key and --google-cx options.
- Added CIDR IP range (e.g. 198.51.100.0/24) and ASN (e.g. AS15169) target parsing and auto-adaptation in 'internal/engine/builder.go'.
- Integrated all new features into 'cmd/dorkforge/main.go', 'internal/recon', and 'internal/recon/export.go'.
- Verified 100% test pass rate across all unit test suites.

Why:
- Expand passive reconnaissance, noise reduction, and continuous DevSecOps audit capabilities while staying strictly within the DorkForge passive search dorking scope.

Files affected:
- internal/ignore/ignore.go — Exclusion rules engine
- internal/ignore/ignore_test.go — Ignore rules unit tests
- internal/diff/diff.go — Report comparison engine
- internal/diff/diff_test.go — Diff comparison unit tests
- internal/api/google.go — Google CSE REST API client
- internal/api/api_test.go — Google CSE API unit tests
- internal/engine/builder.go — Added CIDR and ASN target support
- internal/engine/builder_test.go — Added CIDR and ASN unit tests
- internal/recon/recon.go — Integrated ignore rules and Google CSE API execution
- internal/recon/export.go — Rendered Google CSE results in HTML dashboard
- cmd/dorkforge/main.go — Wired --ignore, --diff, --google-key, and --google-cx flags
- commit.txt — Updated release notes
