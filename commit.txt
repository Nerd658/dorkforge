[2026-08-19] — Add Launch All Button in HTML Reports and CLI --open Browser Execution

What changed:
- Added interactive '🚀 Launch All Visible Dorks' button in standalone HTML dashboards (internal/output/exporter.go and internal/recon/export.go).
- Added client-side JavaScript batching (300ms polite delay per tab) to prevent browser pop-up blockers from swallowing multi-tab query launches.
- Added '--open', '--batch-size', and '--delay' flags to 'dfg recon' command in main.go, enabling direct multi-tab browser opening from terminal.
- Updated unit tests and verified 100% pass rate.

Why:
- Allow security auditors to launch all target dork queries into browser tabs in one click from HTML reports or directly from terminal commands.

Files affected:
- internal/output/exporter.go — Added Launch All button and JS handler
- internal/recon/export.go — Added Launch All button to Recon HTML dashboard
- cmd/dorkforge/main.go — Added --open, --batch-size, --delay to handleRecon
- commit.txt — Updated release notes
