[2026-08-19] — Add Exploit-DB GHDB signatures, shell completion, and benchmark test suite

What changed:
- Expanded dork signatures catalog with Exploit-DB GHDB, modern Cloud (Firebase, Google Sheets, Jira, Notion), Dev frameworks (Spring Boot Actuators, Next.js, Prisma, Terraform tfstate), and AI credentials (OpenAI, Anthropic, Slack, Stripe).
- Added cross-shell autocompletion generator for Bash, Zsh, and Fish via 'dorkforge completion <shell>'.
- Added comprehensive benchmark test suites profiling target sanitization, query rendering, and export serialization (JSON, HTML, Markdown, URLs).
- Added catalog integrity test suite verifying category, severity, engine, and query template conformity.

Why:
- Provide comprehensive exposure coverage aligned with Exploit-DB GHDB standards and ensure high CLI developer experience and verified benchmark throughput.

Files affected:
- cmd/dorkforge/main.go — Added completion subcommand handler and updated usage text
- internal/completion/completion.go — Shell script generators for Bash, Zsh, and Fish
- internal/completion/completion_test.go — Completion generation test suite
- internal/dorks/expanded.go — 48+ new high-value dork signatures
- internal/dorks/catalog_test.go — Catalog integrity test suite
- internal/engine/benchmark_test.go — Performance benchmarks for engine operations
- internal/output/benchmark_test.go — Performance benchmarks for report exporters
- README.md — Updated feature list, shell completion setup instructions, and architecture
- commit.txt — Updated release notes

Breaking changes:
- None

Notes for review:
- Test shell completion with 'source <(./bin/dorkforge completion bash)'.
- Execute benchmarks with 'go test -bench=. -benchmem ./...'.
